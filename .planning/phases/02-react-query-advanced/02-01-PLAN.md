---
phase: 02-react-query-advanced
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useCollection.ts
  - src/app/collection/CollectionPageClient.tsx
autonomous: true
requirements:
  - RQ-04
  - RQ-05

must_haves:
  truths:
    - "Collection page uses React Query with centralized query keys"
    - "Collect/uncollect mutations invalidate collection queries"
    - "Collection page UI updates immediately after collect/uncollect"
  artifacts:
    - path: "src/hooks/useCollection.ts"
      provides: "React Query hooks for collection with optimistic updates"
      exports: "useCollection"
    - path: "src/app/collection/CollectionPageClient.tsx"
      provides: "Collection page using useCollection hook"
  key_links:
    - from: "src/app/collection/CollectionPageClient.tsx"
      to: "src/hooks/useCollection.ts"
      via: "import { useCollection }"
    - from: "src/hooks/useCollection.ts"
      to: "src/lib/query-keys.ts"
      via: "queryKey: ['collection', userId]"
---

<objective>
Verify that the Collection page correctly uses React Query with centralized query keys and proper cache invalidation for collect/uncollect mutations (RQ-04, RQ-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/hooks/useCollection.ts
@src/app/collection/CollectionPageClient.tsx
@src/lib/query-keys.ts

## Verification Checklist

### RQ-04: Collection page uses useQuery
- CollectionPageClient imports and uses `useCollection()` hook (line 4, line 89)
- `useCollection` uses `useQuery` with query key `["collection", userId]` (line 89-93)
- Query key matches `collectionKeys.list(userId)` from query-keys.ts

### RQ-05: Collect/uncollect invalidates queries
- `collectMutation` has `onSettled` that invalidates `["collection", userId]` and `["collection-ids", userId]` (lines 114-115)
- `uncollectMutation` has same `onSettled` invalidation (lines 114-115)
- Both mutations call `emitCollectionUpdate()` for event bus (line 116)
- Both mutations call `checkAndUnlockBadges()` (line 117-118)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify query key alignment</name>
  <files>src/hooks/useCollection.ts</files>
  <action>
    Read useCollection.ts and verify:
    1. Line 89-93: collectionQuery uses queryKey `["collection", user?.id]`
    2. Line 95-99: idsQuery uses queryKey `["collection-ids", user?.id]`
    3. These keys match collectionKeys.list() and collectionKeys.ids() from query-keys.ts

    If keys match (expected): Document this in the plan summary.
    If keys do NOT match: Update to use collectionKeys.list(userId) and collectionKeys.ids(userId) from query-keys.ts.
  </action>
  <read_first>
    - src/hooks/useCollection.ts (current implementation)
    - src/lib/query-keys.ts (expected key format)
  </read_first>
  <acceptance_criteria>
    - grep "collectionKeys.list\|collectionKeys.ids" src/hooks/useCollection.ts returns matches OR
    - grep 'queryKey: \["collection"' src/hooks/useCollection.ts returns queryKey: ["collection", user?.id]
    - grep 'queryKey: \["collection-ids"' src/hooks/useCollection.ts returns queryKey: ["collection-ids", user?.id]
  </acceptance_criteria>
  <verify>
    <automated>grep -n "collectionKeys.list\|collectionKeys.ids\|queryKey: \["collection"\|queryKey: \["collection-ids"" src/hooks/useCollection.ts</automated>
  </verify>
  <done>Query keys verified as aligned with centralized query-keys.ts</done>
</task>

<task type="auto">
  <name>Task 2: Verify invalidation on collect/uncollect</name>
  <files>src/hooks/useCollection.ts</files>
  <action>
    Read useCollection.ts and verify:
    1. Lines 114-115: onSettled invalidates both ["collection", userId] and ["collection-ids", userId]
    2. collectMutation (lines 123-145) has onSettled that calls invalidation
    3. uncollectMutation (lines 147-162) has onSettled that calls invalidation

    Confirm that both mutations invalidate queries after completion.
  </action>
  <read_first>
    - src/hooks/useCollection.ts (check onSettled callbacks)
  </read_first>
  <acceptance_criteria>
    - grep "onSettled" src/hooks/useCollection.ts contains queryClient.invalidateQueries for both collection and collection-ids
    - Both collectMutation and uncollectMutation have onSettled defined
  </acceptance_criteria>
  <verify>
    <automated>grep -n "invalidateQueries.*collection" src/hooks/useCollection.ts</automated>
  </verify>
  <done>Both collect and uncollect mutations properly invalidate collection queries</done>
</task>

</tasks>

<verification>
## RQ-04 Verification
- CollectionPageClient.tsx line 89 uses `useCollection()` hook
- useCollection uses useQuery with queryKey ["collection", user?.id]
- Keys match centralized query-keys.ts format

## RQ-05 Verification
- collectMutation onSettled invalidates ["collection", userId] and ["collection-ids", userId]
- uncollectMutation onSettled invalidates same keys
- Both mutations also trigger badge checks and collection update events
</verification>

<success_criteria>
1. RQ-04: Collection page uses React Query with query key ["collection", userId] - VERIFIED
2. RQ-05: Collect/uncollect mutations call invalidateQueries for both ["collection", userId] and ["collection-ids", userId] - VERIFIED
3. No code changes required - requirements already implemented correctly
</success_criteria>

<output>
After completion, create `.planning/phases/02-react-query-advanced/summaries/02-01-SUMMARY.md`
</output>
