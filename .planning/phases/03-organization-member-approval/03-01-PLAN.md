---
phase: 03-organization-member-approval
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260408130000_oma_schema_member_approval.sql
  - supabase-schema.sql
  - src/lib/types.ts
autonomous: true
requirements:
  - OMA-01
  - OMA-02
must_haves:
  truths:
    - "Organizations can toggle manual approval requirement"
    - "Users requesting to join can have pending status"
    - "Admins can approve or reject pending members"
  artifacts:
    - path: "supabase/migrations/20260408130000_oma_schema_member_approval.sql"
      provides: "Database columns and RLS updates"
      exports: "ADD COLUMN requires_member_approval, ALTER membership_status"
    - path: "src/lib/types.ts"
      provides: "Updated OrganizationMembership interface with pending status"
      contains: "membership_status: 'active' | 'invited' | 'removed' | 'pending'"
---

<objective>
Add database schema changes to support optional manual member approval for organizations.

Purpose: Enable clubs to verify members or require approval before granting membership.
Output: Migration file that adds `requires_member_approval` to organizations and extends `membership_status` with `pending` value.
</objective>

<context>
@supabase-schema.sql
@src/lib/types.ts
@src/lib/roles.ts

## Schema Analysis

**organizations table** (lines 25-38):
- Missing `requires_member_approval` boolean column
- No column to control manual approval workflow

**organization_members table** (lines 72-83):
- `membership_status` currently: `active | invited | removed`
- Need to extend with `pending` value
- `joined_at` column exists — can use for approval timestamp

## Type Analysis

**OrganizationMembership interface** (src/lib/types.ts, line 19-30):
- `membership_status` typed as `'active' | 'invited' | 'removed'`
- Must extend to include `'pending'`

## Migration Approach

1. Add `requires_member_approval BOOLEAN DEFAULT false` to organizations
2. ALTER membership_status CHECK constraint to include `pending`
3. Existing members remain `active` (no migration of data needed)
4. RLS policy: pending members cannot see org data until approved
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration file for member approval schema</name>
  <files>supabase/migrations/20260408130000_oma_schema_member_approval.sql</files>
  <action>
Create migration file with the following SQL:

1. Add `requires_member_approval BOOLEAN DEFAULT false NOT NULL` column to `organizations` table after `logo_url` column

2. ALTER the CHECK constraint on `organization_members.membership_status`:
   - Drop existing CHECK constraint (named likely `organization_members_membership_status_check`)
   - Add new CHECK: `(membership_status IN ('active', 'invited', 'removed', 'pending'))`

3. Add index on `(organization_id, membership_status)` for efficient pending member queries:
   `CREATE INDEX idx_org_members_status ON organization_members(organization_id, membership_status);`

4. Add rejection reason column (optional for transparency):
   `rejection_reason TEXT` to organization_members table

5. Update RLS policy for pending members visibility:
   - Pending members should NOT be visible in the public member list
   - Only admins can see pending members
   - Modify the "Members can view org members" policy to add: `AND membership_status != 'pending'`
   - Add new policy: "Admins can view pending members" with: `membership_status = 'pending' AND role IN ('admin', 'gründer')`

Note: Check constraint names by running: `SELECT conname FROM pg_constraint WHERE conrelid = 'organization_members'::regclass;`
</action>
  <verify>
`supabase db push` executes without error, or run against local database to verify SQL syntax.
</verify>
  <done>
Migration creates `requires_member_approval` column on organizations, extends membership_status with pending, creates index, and updates RLS policies for proper pending member visibility.
</done>
</task>

<task type="auto">
  <name>Task 2: Update types.ts with new membership statuses</name>
  <files>src/lib/types.ts</files>
  <action>
Update the OrganizationMembership interface (line 19-30) to:

1. Change `membership_status` type from `'active' | 'invited' | 'removed'` to `'active' | 'invited' | 'removed' | 'pending'`

2. Add optional `rejection_reason?: string | null` field to OrganizationMembership interface

3. Update Organization interface (lines 6-17) to add `requires_member_approval?: boolean` after `logo_url?` field

Use exact literal types with pipe union syntax matching existing patterns.
</action>
  <verify>
`npm run build` succeeds with no TypeScript errors
</verify>
  <done>
TypeScript types updated to reflect pending membership status and requires_member_approval flag.
</done>
</task>

<task type="auto">
  <name>Task 3: Update supabase-schema.sql with new columns and policies</name>
  <files>supabase-schema.sql</files>
  <action>
Update the schema file to match the migration:

1. In organizations table (lines 25-38), add `requires_member_approval BOOLEAN DEFAULT false` after `logo_url TEXT`

2. In organization_members table (lines 72-83), update the CHECK constraint:
   - Change from: `CHECK (membership_status IN ('active', 'invited', 'removed'))`
   - To: `CHECK (membership_status IN ('active', 'invited', 'removed', 'pending'))`

3. Add `rejection_reason TEXT` column after `updated_at TIMESTAMPTZ`

4. Update the RLS policy comment for "Members can view org members" (line 91-98) to clarify that pending members are excluded

5. Add a new policy statement in comments: "Admins can view and manage pending members"

6. Add index: `CREATE INDEX idx_org_members_status ON organization_members(organization_id, membership_status);` after existing indexes

Keep all other existing columns and constraints unchanged.
</action>
  <verify>
Schema file updated with new columns, check constraint updated, and index added. All existing definitions preserved.
</verify>
  <done>
supabase-schema.sql reflects the new member approval schema with requires_member_approval flag and pending membership status.
</done>
</task>

</tasks>

<verification>
- Migration file created at correct path with timestamp prefix
- Database schema updated with requires_member_approval column
- membership_status extended with pending value
- Index created for efficient pending member queries
- RLS policies updated to handle pending member visibility
- TypeScript types updated to reflect changes
- npm run build passes with no errors
</verification>

<success_criteria>
1. OrganizationMembership type has membership_status including 'pending'
2. Organization type has requires_member_approval boolean
3. organization_members table has rejection_reason column
4. Index idx_org_members_status exists on (organization_id, membership_status)
5. RLS policies properly restrict pending member visibility
</success_criteria>

<output>
After completion, create `.planning/phases/03-organization-member-approval/03-01-SUMMARY.md`
</output>