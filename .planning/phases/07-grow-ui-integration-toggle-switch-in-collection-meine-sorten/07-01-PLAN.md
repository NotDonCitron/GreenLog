# Phase 7 Plan: Grow-UI-Integration

**Goal:** Integrate Grow features into existing navigation without adding 6th tab. Toggle switches split content semantically across existing tabs.

---

## Task 1: Toggle-Switch in Collection (Meine Sorten / Meine Grows)

### Files to Modify
- `src/app/collection/CollectionPageClient.tsx`

### Steps
1. Add `collectionTab` state (`"strains"` | `"grows"`)
2. Add Segmented Control toggle between "Meine Sorten" and "Meine Grows" under header
3. When `"strains"` → render existing collection UI
4. When `"grows"` → render inline Grow list (moved from `/grows`)
5. Add "Neues Grow" FAB link → `/grows/new`

### Design
- Segmented control: `flex gap-2 p-1 bg-[var(--muted)] rounded-2xl`
- Active tab: `bg-[#2FF801] text-black shadow-lg shadow-[#2FF801]/20`
- Inactive tab: `text-[var(--muted-foreground)] hover:text-[var(--foreground)]`

### Success Criteria
- [ ] Toggle switch visible under header
- [ ] "Meine Sorten" shows existing strain collection
- [ ] "Meine Grows" shows grow list inline
- [ ] FAB for new grow visible in both modes
- [ ] Active grow count badge on "Meine Grows" tab

---

## Task 2: Quick-Access Widget on Home Dashboard

### Files to Modify
- `src/app/page.tsx` (HomeContent)

### Steps
1. Create `ActiveGrowCard` component showing top active grow
2. Place prominent card under header, above Strain of the Day
3. Show: grow title, strain name, current phase/day, link to `/grows/[id]`

### Design
- Card with `bg-gradient-to-br from-[#2FF801]/10 to-transparent` border
- Sprout icon + phase badge
- Clickable → navigates to `/grows/[id]`

### Success Criteria
- [ ] Active grows widget visible on home screen
- [ ] Shows grow title + current phase + day count
- [ ] Clicking navigates to grow detail

---

## Task 3: Toggle-Switch in Social (Community Feed / Public Grows)

### Files to Modify
- `src/app/feed/page.tsx` (FeedContent)

### Steps
1. Add `socialTab` state (`"feed"` | `"grows"`)
2. Replace existing tab bar with unified toggle (Community Feed | Public Grows)
3. When `"feed"` → existing ActivityFeed content
4. When `"grows"` → inline ExploreGrowsPage content + KCanGDisclaimer
5. Move Community Feed toggle to secondary sub-tab inside "feed" mode

### Design
- Unified toggle: same segmented control pattern
- Active: `bg-[#00F5FF] text-black`
- Secondary sub-toggle inside feed mode: "Für dich | Following"

### Success Criteria
- [ ] Social tab shows Community Feed / Public Grows toggle
- [ ] "Public Grows" shows grow explore feed with KCanG disclaimer
- [ ] Existing feed modes accessible in secondary toggle

---

## Dependencies
- Tasks 2 and 3 require Task 1 for shared component patterns

## Verification
1. `npm run build` exits 0
2. Toggle switches work correctly
3. No duplicate BottomNav
4. No console errors
